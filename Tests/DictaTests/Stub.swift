// A stub, on purpose. Under Command-Line-Tools-only `swift test` COMPILES this bundle but does not
// EXECUTE it — there is no `xctest` host utility, so a failing test here would still exit 0.
// Never put a real assertion in this target: it would report as passing while never having run.
// The real tests live in Sources/DictaTestRunner and run via `bash Scripts/test.sh`.
