# dicta — steps 1–3 of SPEC.md

## Overview

Build dicta from an empty repository (spec only) up to and including step 3 of `SPEC.md`'s acceptance criteria:

- **Step 1** — the whole delivery path with fakes: chord → `dictactl` → daemon → `agtermctl session type`, canned hostile transcript, nothing submitted.
- **Step 2** — the microphone and the models: a signed `.app` that owns the TCC grant, real capture at 16 kHz mono, capture faults, and a Parakeet recogniser warm before the first chord.
- **Step 3** — the text pipeline: the append-only record, reading it back, and the Tier 0 replacement dictionary, with a misfiring rule diagnosable from the record alone.

Out of scope here, deliberately: **step 4** (the external filter — it needs a decision the user wants to make after seeing real recogniser output, D9c) and **step 5** (the sweep that gives every §7 row a test or a checklist entry). Rows of §7 that belong to a component built here are still tested as that component is built; what step 5 adds later is the *audit* that none was missed.

An earlier step-1 skeleton was built, measured, verified end to end and deleted; it survives as commit `3dda6cb`. It is a reference for target layout and for two of the spec's measurements (F4, F5), not something to restore wholesale — the spec has moved on since (`warming` is now a first-class state, the record has a schema, the failure vocabulary is sharper).

### One deliberate departure from the spec's step numbering

§10 files the record under step 3, but step 2's criterion (b) is scored by reading `recognised` out of the record, and invariant 10 ("recognised text always reaches the record before injection") is cheapest to establish while the transcriber is still a fake that returns on demand. The record is therefore built in Task 7, before the microphone. Nothing else moves.

## Context

- Files involved: the repository is empty apart from `SPEC.md` and `docs/`. Everything below is new.
- Reference material:
  - `SPEC.md` — normative. Decisions cited as D*, measurements as F*, invariants as §8.
  - `git show 3dda6cb:<path>` — the deleted skeleton. Directly reusable in intent: `Package.swift` (the target graph and `swiftTestingSettings()`), `Scripts/test.sh`, `CLAUDE.md`, and the shape of `Sanitizer`/`StateMachine`/`ControlSocket`/`Agterm`.
  - `../acta/Scripts/setup-signing.sh` — a non-interactive, dedicated-keychain, self-signed identity, so the designated requirement is stable and a TCC grant survives a rebuild.
  - `../acta/Scripts/bundle.sh` — SwiftPM → `.app` under Command Line Tools only.
- Related patterns:
  - A decision is a pure value in `DictaCore`; its I/O in `DictaRuntime`; its test in `DictaTestRunner` (D19).
  - `swift test` is **not** a gate under CLT-only (D18); `bash Scripts/test.sh` is.
  - **English only, with no exceptions** — code, comments, docs, scripts, commit messages (Conventional Commits), notifications, `dictactl` output, test names, `NSMicrophoneUsageDescription`. This follows SPEC.md's preamble, which is normative and supersedes the earlier "user-facing strings are Russian" carve-out the deleted skeleton used; Task 12's Cyrillic grep is only a gate when there is nothing legitimate for it to find. Conversation with the user stays Russian.
- Dependencies:
  - FluidAudio via SwiftPM (Parakeet TDT 0.6B v3, CoreML/ANE) — D10, added only in Task 10.
  - `agtermctl` at runtime; a local self-signed code-signing identity for the daemon bundle (D11).
  - Environment (from the skeleton's `CLAUDE.md`, re-verify in Task 1): Swift 6.3.2, `arm64-apple-macosx26.0`, **Command Line Tools only, no full Xcode**.

## Development Approach

- **Testing approach**: **TDD** for everything in `DictaCore` — the state machine, the sanitiser, the record schema and the replacement engine are pure values, and their tests *are* the executable form of §6/§8/§9. **Regular** (code first, then tests) for `DictaRuntime`, where a test drives fakes behind the seams and the fake has to exist first.
- Every seam — `Capture`, `Transcriber`, `Filter`, `Injector`, `Notifier`, `Clock` — has a fake, so the entire lifecycle is drivable with no microphone, no model and no terminal (D19).
- `FakeTranscriber` returns deliberately hostile canned text (a newline, a double space, a trailing space) so a bypassed sanitiser fails loudly at the first end-to-end run, not in production.
- Complete each task fully before moving to the next.
- **CRITICAL: every task MUST include new/updated tests.**
- **CRITICAL: all tests must pass (`bash Scripts/test.sh`) before starting the next task.**
- Hardware, TCC prompts, real speech and real chords cannot be asserted by a test. They are collected in the Post-Completion checklist and never appear as task checkboxes.

## Implementation Steps

### Task 1: Scaffolding, and a test gate proven able to fail

**Files:**
- Create: `Package.swift`, `.gitignore`, `CLAUDE.md`
- Create: `Scripts/test.sh`, `Scripts/run.sh`, `Scripts/install.sh`, `Scripts/lint.sh`, `.swiftlint.yml`
- Create: `Sources/DictaTestRunner/main.swift`, `Tests/DictaTests/Stub.swift`

- [x] write `Package.swift` with the target graph: `DictaCore` (pure), `DictaIPC` (→ `DictaCore`), `DictaRuntime` (→ `DictaCore`, `DictaIPC`), `Dicta` (daemon executable), `dictactl` (→ `DictaCore` + `DictaIPC` **only** — D12, §8.8), `DictaTestRunner`, and the compile-only `DictaTests` stub; include `swiftTestingSettings()` for the CLT-only framework/plugin paths
- [x] write `Scripts/test.sh` (`swift run DictaTestRunner`), `run.sh` (daemon in the foreground, logs to stderr), `install.sh` (release build → `~/.local/bin`), `lint.sh`
- [x] verify the toolchain assumptions the plan rests on and record the actual values in `CLAUDE.md`: `swift --version`, whether full Xcode is present, that `agtermctl` is on PATH
- [x] add a deliberately failing test; confirm `bash Scripts/test.sh` exits non-zero **and** `swift test` exits 0; record that contrast in `CLAUDE.md` as the reason the runner exists (D18), then delete the failing test
- [x] write `CLAUDE.md`: language convention, commands, environment facts (F2–F4), the structure rule, and the "rules worth not relearning" list distilled from §3
- [x] write a first real test asserting the runner's entry point executes
- [x] run `bash Scripts/test.sh` — must pass before Task 2

### Task 2: DictaCore — wire types, paths, and the sanitiser

**Files:**
- Create: `Sources/DictaCore/Wire.swift`, `Sources/DictaCore/Paths.swift`, `Sources/DictaCore/Sanitizer.swift`
- Create: `Sources/DictaTestRunner/SanitizerTests.swift`, `Sources/DictaTestRunner/WireTests.swift`

- [x] define the wire types: commands (`status`, `toggle`, `start`, `stop`, `abort`, `last`), the mode (`clean` | `raw`), the lifecycle state (`idle`, `warming`, `recording`, `processing`, `injecting`), responses carrying a `message`, an outcome kind that distinguishes **rejection** (audible) from **no-op** (silent) per §6, and an explicit maximum frame size
- [x] carry the attempt id and the target (session id, pane) on the wire, so a command naming a spent attempt is recognisable as such rather than guessed at
- [x] define `Paths`: socket, record file, dictionary file, config — all under `~/Library/Application Support/dev.personal.dicta`, with the directory created on demand
- [x] write the sanitiser tests **first**: `\n`, `\r`, `\r\n`, `U+0085`, `U+2028`, `U+2029` all removed; runs of whitespace collapsed to one space; leading/trailing trimmed; the result contains no line break of any kind (§8.2); text that is empty *after* sanitising is reported as empty rather than injected; a string of only whitespace is empty; non-ASCII (Cyrillic, emoji) survives untouched
- [x] implement `Sanitizer.sanitize` as a pure function returning either a single-line string or "empty", so the caller cannot forget the empty case
- [x] write wire round-trip tests: every command and response encodes and decodes, an unknown command is a clean decode error rather than a crash, and a frame over the limit is rejected
- [x] run `bash Scripts/test.sh` — must pass before Task 3

### Task 3: DictaCore — the lifecycle state machine

**Files:**
- Create: `Sources/DictaCore/StateMachine.swift`
- Create: `Sources/DictaTestRunner/StateMachineTests.swift`

- [x] write the tests **first**, one per cell of §6's table — `idle | warming | recording | processing | injecting` × `start | stop | abort` — each named after its cell, so a missing cell is visible in the test list
- [x] test the cells the table makes non-obvious: stop during `warming` **cancels** with nothing delivered (no audio exists yet); abort during `processing` cancels and injects nothing; abort during `injecting` is **refused** (D20); stop during `processing` or `injecting` is a *quiet* no-op, not an error and not a second delivery (§8.5)
- [x] test that a rejection is audible and a no-op is silent, and that the two are different values in the result rather than the same value read two ways
- [x] test that attempt ids are monotonic and never reused, and that a command naming a spent id is a no-op in every state
- [x] test that late callbacks never resurrect an abandoned attempt: capture confirming readiness after an abort, recognition returning after a cancel, injection completing after a fault
- [x] test that leaving `recording` requires capture to have been drained (§8.9) — a second start while the first attempt is still stopping is rejected, not queued
- [x] test that the toggle resolves to start-or-stop **inside** the machine (D7), and that resolving it twice from the same state cannot yield both
- [x] implement `StateMachine` as a pure value: event in, `(newState, effects)` out; effects are data (`beginCapture`, `announce`, `inject`, `notify`), never closures, so a test can assert on them
- [x] run `bash Scripts/test.sh` — must pass before Task 4

### Task 4: The socket transport and the keypress client

**Files:**
- Create: `Sources/DictaIPC/ControlSocket.swift`
- Create: `Sources/dictactl/main.swift`
- Create: `Sources/DictaTestRunner/ControlSocketTests.swift`
- Create: `docs/keymap.snippet.conf`

- [x] implement both halves of the Unix-socket transport in one file, so the two ends' framing rules cannot drift: JSON-lines framing, the frame-size limit from Task 2, a connect timeout and a read timeout that make an unresponsive daemon a fast local failure
- [x] implement `dictactl` with `toggle --mode <clean|raw> --session <id> --socket <path>`, plus `start`, `stop`, `abort`, `status`, `last`; it links `DictaCore` + `DictaIPC` and nothing else (D11, D12)
- [x] make "cannot reach the daemon" a **loud** local failure — a desktop notification, not only a non-zero exit and a line on stderr (§7); the user's hands are on the keyboard, not on a log
- [x] handle a stale socket file (no listener behind it) distinctly from "no socket at all", since the first means a crashed daemon and the message should say so
- [x] write transport tests over a real socket pair in a temp directory: round trip; oversized frame rejected without reading it all; truncated frame rejected; garbage frame rejected; no daemon listening; stale socket file; a client that disconnects mid-request does not wedge the server
- [x] write `docs/keymap.snippet.conf`: the three chords (`⌃⌥D`, `⌃⌥⇧D`, `⌃⌥X`) invoking `dictactl` **by absolute path and not through a login shell** (§12), passing `$AGT_SESSION_ID` and `$AGT_SOCKET`, both start chords calling the same toggle verb (D5, D7, §6)
- [x] run `bash Scripts/test.sh` — must pass before Task 5

### Task 5: The agterm adapter, the seams, and their fakes

**Files:**
- Create: `Sources/DictaRuntime/Agterm.swift`, `Sources/DictaRuntime/Seams.swift`, `Sources/DictaRuntime/Fakes.swift`
- Create: `Sources/DictaTestRunner/AgtermTests.swift`

- [x] define the seams as protocols: `Capture`, `Transcriber`, `Filter`, `Injector`, `Notifier`, `Clock`; the `Filter` seam ships as a `NoFilter` pass-through (D9c — step 4 fills it, and the seam exists now only because raw mode is *defined* as skipping this stage)
- [x] write the fakes: a `FakeCapture` whose readiness, drain and faults are driven by the test; a `FakeTranscriber` returning deliberately hostile canned text containing a newline, a double space and a trailing space; a `FakeInjector` recording exactly what it was handed; a `FakeClock` so the watchdog and the duration cap are testable without waiting
- [x] implement `Agterm`: pane resolution from `agtermctl tree --json` (the active surface of the session), `session type` for injection, `session status` for indicator and sound, and desktop notifications
- [x] implement target resolution per §5 — session id from the keypress, pane from live focus — and **fail closed**: if the tree does not name exactly one recognisable active pane for that session, the attempt does not start (D6)
- [x] implement re-validation of *both* halves immediately before injection; a target that no longer resolves is a delivery failure and is never re-aimed at whatever has focus now (D4, §8.3)
- [x] write tests against canned `tree --json` payloads: exactly one active pane; no active pane; two candidates; the session absent; malformed JSON; `agtermctl` exiting non-zero; `agtermctl` missing from PATH entirely
- [x] run `bash Scripts/test.sh` — must pass before Task 6

### Task 6: The daemon — step 1 end to end, with fakes

**Files:**
- Create: `Sources/DictaRuntime/Daemon.swift`, `Sources/Dicta/main.swift`
- Create: `Sources/DictaTestRunner/DaemonTests.swift`

- [x] wire the daemon: socket server, the pure state machine, the seams, and the attempt lifecycle `idle → warming → recording → processing → injecting → idle`
- [x] implement §6's feedback table via `session status`: `active --blink` red + `Pop` on listening, `active` amber while working, `completed --auto-reset` + `Tink` on done, `blocked` + notification + `Basso` on empty, faulted or refused
- [x] fire **none** of that until capture confirms it is running (D13, §8.4) — the test asserts the ordering of effects, not merely that they all happened
- [x] refuse to start a second instance while the socket is live, and reset the known target's indicator at startup so a crash cannot leave a light claiming a recording that is not happening (§7)
- [x] add the watchdog that turns a wedged attempt into a capture fault, driven by the injected `Clock` so its test does not sleep
- [x] write daemon tests that drive whole attempts through the fakes: the clean path; the raw path; abort at each state; duplicate start; duplicate stop; target gone at injection; `session type` failing before the first keystroke; `session type` failing after keystrokes have begun (the notification must say the insertion **may be partial**, and there must be no retry)
- [x] assert on every path — clean, raw, and every failure path that still injects — that the injected string is exactly the sanitised text and contains no line break (§8.1)
- [x] run `bash Scripts/test.sh` — must pass before Task 7
- [x] **step 1 is complete here**: the path exists end to end with fakes, and the hostile canned transcript makes a bypassed sanitiser fail immediately

### Task 7: The record (§9), and reading it back

**Files:**
- Create: `Sources/DictaCore/Record.swift`, `Sources/DictaRuntime/History.swift`
- Create: `Sources/DictaTestRunner/RecordTests.swift`
- Modify: `Sources/DictaRuntime/Daemon.swift`, `Sources/dictactl/main.swift`

*Built before step 2 because step 2's criterion (b) is scored by reading `recognised` out of it, and invariant 10 is cheapest to establish while the transcriber is still a fake.*

- [x] write the tests **first** for the entry as a pure codable value with every §9 field: `id`, `at`, `outcome` (all eleven listed values), `mode`, `recognised`, `final`, `rules`, `target`, `error`
- [x] implement the append-only writer: one entry per attempt, written **before** injection is attempted, so a delivery failure cannot lose recognised text (§8.10)
- [x] record an entry even for attempts that never reached recognition — outcome and reason with no text is the honest record, not a gap (D17)
- [x] make a **failing** history append still allow an otherwise-safe injection, followed by a loud notification that recovery is unavailable — property 2 is what just broke (§7)
- [x] implement `dictactl last`: prints `final` by default and `recognised` verbatim behind a flag; reading back is not injection, so the sanitiser does not apply (§9, §8.1's parenthesis)
- [x] write tests: exactly one entry per attempt across every outcome reachable so far; append-only under concurrent writers; ids monotonic and ordering preserved; a truncated or corrupt trailing line does not prevent reading the entries before it; `recognised` and `final` round-trip independently, including when they differ
- [x] run `bash Scripts/test.sh` — must pass before Task 8

### Task 8: The signed bundle — the anchor the TCC grant attaches to

**Files:**
- Create: `Resources/Info.plist`, `Resources/Dicta.entitlements`
- Create: `Scripts/setup-signing.sh`, `Scripts/bundle.sh`, `Scripts/launchagent.plist`
- Modify: `Scripts/install.sh`, `CLAUDE.md`

*Deliberately its own task, before any audio code exists: if the bundle identity is wrong, every microphone measurement afterwards is taken against a grant that will evaporate on the next rebuild.*

- [x] port acta's signing recipe: a self-signed certificate in a dedicated, empty-password keychain with the partition list set so `codesign` never prompts, making the designated requirement stable across rebuilds (D11); idempotent, so `bundle.sh` may call it freely
- [x] write `Scripts/bundle.sh`: SwiftPM release build → `Dicta.app`, bundle id `dev.personal.dicta`, `LSUIElement` true, `NSMicrophoneUsageDescription` **in English** — SPEC.md's preamble is English-only with no exceptions and names this key explicitly, and `Scripts/lint.sh`'s Cyrillic grep would fail on a Russian one; the "in Russian" above is a leftover from the deleted skeleton's carve-out — no dock icon, no menu bar, no windows (D11, §13)
- [x] install the daemon as a user LaunchAgent (`KeepAlive`, `RunAtLoad`, stderr to a log file) and make `Scripts/install.sh` bundle, sign, install and reload it in one command — the template's substitution and `plutil -lint` were verified against a rendered copy; actually bootstrapping the agent adds a login item to this machine and is left to the user (it is already a Post-Completion item)
- [x] verify the resulting requirement is identity-based rather than cdhash-based (`codesign -d -r- Dicta.app`), and that rebuilding twice yields the same requirement string — measured: a changed string literal moved the cdhash `6764650c…` → `acc55018…` while the requirement stayed byte-identical; the ad-hoc contrast (`cdhash H"…"`) was measured too, and `bundle.sh` now fails on it
- [x] write a test asserting the plist and entitlements files are well-formed and carry the exact keys the grant depends on (bundle id, `LSUIElement`, the usage description), so a later edit cannot quietly drop one — `Sources/DictaTestRunner/BundleTests.swift`, probed by deleting `LSUIElement` and misspelling the bundle id
- [x] run `bash Scripts/test.sh` — must pass before Task 9

### Task 9: Real capture, capture faults, and the duration cap

**Files:**
- Create: `Sources/DictaRuntime/AudioCapture.swift`, `Scripts/measure.sh`
- Create: `Sources/DictaTestRunner/AudioCaptureTests.swift`
- Modify: `Sources/DictaRuntime/Daemon.swift`, `Sources/DictaCore/StateMachine.swift` (fault events, if needed)

- [x] implement `Capture` over AVAudioEngine: input tap converted to 16 kHz mono float, buffered in memory only — no segmentation, no disk journal, no recovery pass (D14) — verified against the real device with a throwaway probe: 2.9 s of speech arrived as 46 410 samples at 16 kHz with signal in them, which no unit test can establish
- [x] confirm "recording" only once the engine is actually running, and expose a distinct state for a **denied** microphone, so `warming` can be told apart from ready and from denied (§6) — `.ready` is emitted after `engine.start()` returns *and* `isRunning` is true; `MicrophoneAccess` is the third answer, and `FaultKind.denied` is what a chord gets instead of a device
- [x] implement the capture faults, each discarding the audio and injecting nothing (D16, §8.6): audio interruption, sleep/wake, input device or route change, engine death, and the ten-minute duration cap — macOS has no `AVAudioSession`, so interruption, device and route changes all arrive as `AVAudioEngineConfigurationChange`; sleep comes from `NSWorkspace`, which is why `main.swift` now runs a run loop rather than `dispatchMain()`
- [x] make every fault report as a **hardware fault**, never as silence (§8.7) — a fault while *stopping* is the row most likely to be mistaken for an empty dictation — `AudioCapture.drainOutcome` is where that is decided, pure and tested: a fault in hand wins over the samples in hand, and a stopped engine is a fault even with a full buffer
- [x] make the cap still record whatever text was produced, with outcome `capped` (D15) — the cap lives in the daemon, not in capture: it must outlast the device (a drain that wedges at 9:59 is still a capped attempt) and it is the only place with the injected `Clock`
- [x] write `Scripts/measure.sh`: N consecutive toggles, reporting client-invocation → "recording" per attempt and the distribution, so step 2's criterion (a) (<150 ms warm, 10 attempts) is scored from numbers rather than impressions — every attempt is ended with `abort`, so measuring types nothing into a pane, and a test parses the script's verbs and flags with `ClientCommand` so it cannot drift
- [x] write tests through the capture seam for every fault path — fault while recording, fault while stopping, cap firing during processing, a fault arriving after an abort (must not resurrect it) — plus a conversion test over a synthesised buffer asserting sample rate, channel count and sample values — each new rule was probed by making it fail (cap disarmed, samples winning over a fault, the denied check removed, a renamed flag)
- [x] run `bash Scripts/test.sh` — must pass before Task 10 — 288 tests, and `bash Scripts/lint.sh` clean

### Task 10: FluidAudio Parakeet, warm at daemon start

**Files:**
- Create: `Sources/DictaRuntime/ParakeetTranscriber.swift`
- Create: `Sources/DictaTestRunner/TranscriberTests.swift`
- Modify: `Package.swift`, `Sources/DictaRuntime/Daemon.swift`, `Sources/Dicta/main.swift`, `Scripts/measure.sh`

- [x] add FluidAudio as a pinned SwiftPM dependency, linked as a library rather than shelled out to, because the CLI pays model load per invocation and D10's "no model load on the hot path" would fail (§12) — `exact: "0.15.5"`, not `from:`, because the released API is not the documented one: `transcribe` takes an `inout TdtDecoderState` the README omits, and `AsrModels.load` wants the staged repo folder rather than its parent
- [x] load the models at daemon start and run one dummy inference, so the first real dictation pays no ANE compilation; the attempt path loads nothing (D10) — on a real `Thread` after the socket is bound, since the load blocks and the socket must answer meanwhile; measured 17 s on a binary's first run (CoreML compiling the int8 encoder, cached per binary) and 0.2 s on every later one, dummy inference 0.07–0.13 s
- [x] implement the `Transcriber` seam over Parakeet TDT 0.6B v3 with automatic language identification, returning verbatim text as the **recognised** stage — `language: nil` is the automatic LID, a fresh `TdtDecoderState` per utterance so the previous dictation cannot bias this one, and a throwaway probe confirmed real words out of real audio: 5.6 s in 0.11 s, 66.8 s in 0.42 s, Russian correct with punctuation and English terms transliterated into Cyrillic (which is D9a's premise, measured)
- [x] handle §7's recogniser rows: the recogniser throwing or the model being unavailable → no injection, notify, record the error with no text; whitespace-only output → "empty", no injection; output that is not valid UTF-8 or exceeds the frame limit → no injection, record the byte length and the error — the third row is `RecognisedText` in `DictaCore`, judged before the draft is touched, and its ceiling is `Wire.maxFrameBytes` on purpose: text in the record that cannot travel back through the socket is a promise `dictactl last` cannot keep
- [x] add a startup self-check that reports loudly when the models are missing, so that failure is visible before the first chord rather than during it — it names the individual absent files (an interrupted download is the likely shape) and the remedy, `Dicta --fetch-models`, which is a separate one-shot invocation because a LaunchAgent must not pull 600 MB at login unasked
- [x] extend `Scripts/measure.sh` to report stop → injection, so criterion (c) (<2 s for a 60-second utterance) is also scored from numbers — behind `--stop`, off by default and announced when on: the interval ends at the last keystroke, so measuring it *delivers*, and a test asserts both that the default is off and that the verbs and flags still exist
- [x] write tests via the seam for each of those outcomes, plus one asserting that no model load occurs anywhere on the attempt path — a `CountingEngine` behind `RecognitionEngine` turns that into an assertion about a number (one load, six inferences over five attempts), asserted both in isolation and through a whole `Daemon`; a load still in flight is covered too, since a chord that arrives then must wait rather than lose recorded audio
- [x] run `bash Scripts/test.sh` — must pass before Task 11 — 309 tests, `bash Scripts/lint.sh` clean, and the signed bundle rebuilt and warmed to confirm the designated requirement is unchanged
- [x] **step 2's automatable half is complete here**; (a)–(d) are scored by a human in Post-Completion, using `Scripts/measure.sh` and the record

### Task 11: Tier 0 — the replacement dictionary

**Files:**
- Create: `Sources/DictaCore/Replacements.swift`, `docs/replacements.example.conf`
- Create: `Sources/DictaTestRunner/ReplacementsTests.swift`
- Modify: `Sources/DictaRuntime/Daemon.swift`, `Sources/DictaCore/Record.swift` (the `rules` field's contents)

- [x] design the file format: ordered rules, each with a stable id that survives editing the file, matching case-insensitive and word-boundary aware — chosen so a human editing the file can predict what a rule will do (D9a), with the semantics stated in a comment at the head of the example file — `id | pattern | replacement`, one rule per line, literal patterns and not a regex (a misfiring literal is findable by reading the file), the id written by the user so inserting a rule renumbers nothing, and a boundary required only on a side where the pattern's own edge is a letter or a digit — which is the predictable half of `\b` without the half that makes a pattern ending in `.` never fire
- [x] write the tests **first**: rule ordering (an earlier rule's output is visible to a later one, or is not — whichever the format states, asserted either way); overlapping rules; word boundaries in mixed Russian/English text; case handling; a rule whose replacement contains a newline (the sanitiser must still catch it downstream); one malformed rule isolating only itself; the `rules` field naming exactly the rules that fired — the format states that rules **cascade**, and that is asserted as such; three of these tests failed first and changed the implementation (a CRLF file does not split on `"\n"`, because `"\r\n"` is one Swift grapheme cluster; a line break in a replacement has to be U+2028, since a literal newline ends the rule; and a pattern cannot overlap itself inside a word, because the boundary rule refuses both occurrences)
- [x] implement the parser and the engine as pure functions in `DictaCore`; applying returns the replaced text, the ids of the rules that fired, and the dictionary's version or mtime — `Replacements.parse`/`apply`, with `FileDictionary` in `DictaRuntime` supplying the mtime in the record's own timestamp format and re-reading per attempt, so an edited rule fires on the next chord without a restart
- [x] apply **replaced** in *both* modes, before the filter seam and before the sanitiser (§2) — asserted by what the filter was handed: the replaced text, still carrying the newline and the double space, which is why the sanitiser cannot move earlier; probed by skipping the dictionary in raw mode
- [x] handle degradation: a missing, unparsable or partly malformed dictionary skips only the offending rules, applies the rest, and notifies **once** that the dictionary is degraded — it never blocks an injection over a config file (§7) — with one deliberate distinction: a file that has never existed is **not** degraded and is silent, because absence is how a user with no dictionary says so and a notification on every dictation would train them to dismiss dicta's; a file that exists and cannot be read or parsed is loud
- [x] treat a replacement that empties the text as "empty": no injection, notify — the dictionary must not silently delete a dictation (§7) — `RecognitionResult.empty` now carries an optional reason, because "nothing was recognised" for a dictation the recogniser heard and a rule deleted sends the user to their microphone over a rule they wrote
- [x] write `docs/replacements.example.conf` with real entries from this user's vocabulary and one commented-out rule showing the diagnosable-misfire workflow step 3 is scored on — its Cyrillic patterns are the one exemption in `Scripts/lint.sh`'s Cyrillic gate, named as a single path rather than a category, and the gate fails if that file ever stops existing
- [x] run `bash Scripts/test.sh` — must pass before Task 12 — 354 tests, `bash Scripts/lint.sh` clean

### Task 12: Verify acceptance criteria

- [x] run the full test suite: `bash Scripts/test.sh` — 361 tests in 18 suites, green
- [x] run the linter: `bash Scripts/lint.sh` — clean over 62 files, 41 of them Swift
- [x] verify test coverage meets 80%+ for `DictaCore` — **96.25% of lines, 94.03% of regions**, measured by `Scripts/coverage.sh`, which is new. `swift test --enable-code-coverage` is no more usable here than `swift test` is (D18): the instrumented bundle is built and never run, so the profile never exists. The script drives the profile through `DictaTestRunner`'s own entry point instead and reports with the CLT's own llvm-cov, with an 80% floor it exits non-zero under — probed with `--floor 99`. No floor is placed on the other targets: DictaRuntime is seams over AVFoundation, CoreML and subprocesses, and a percentage there would count how much of the world's surface a fake name-checks
- [x] assert at the linker level that `dictactl` binds no AVFoundation, CoreML or AppKit symbols (`otool -L` / `nm`) — §8.8 and D12 are budgets, and a budget with no check drifts — `Scripts/linkage.sh`, run **first** by `Scripts/test.sh` because no swift-testing assertion can reach it (the test runner deliberately links all three). Three assertions: the load commands, the undefined symbols, and DictaRuntime's own mangled names — the dependency edge itself, read back off the binary. Probed against `DictaTestRunner`, where all three fire; that probe also found a `set -o pipefail` + `head` SIGPIPE bug that was silently ending the script before its third check
- [x] cross-check each invariant in §8 against the test that would fail if it stopped holding, and list the mapping in `docs/manual-checklist.md`; any invariant with no test is a gap to close before this task passes — all ten are accounted for. **Invariant 8 was the one gap** and `Scripts/linkage.sh` is what closed it: "the keypress client never opens the microphone" is a property of a linked binary, not of a value
- [x] cross-check each §7 row owned by steps 1–3 against a test or a checklist entry; explicitly mark the filter rows as belonging to step 4 rather than leaving them unaccounted — all eighteen rows have a line. Two are honestly parked on a human (H1, the client's own desktop notification, which lives in an executable target SwiftPM cannot import; H3, the OS wiring of sleep and route changes), and the filter row's *timeout* and *returns-empty* triggers are marked step 4 — the daemon falls back only on a throw today, and there is no subprocess to time out. **The mapping is itself checked**: `ChecklistTests` parses SPEC.md and the checklist against each other and against the suite's own `@Test` names, so a renamed test, a reworded invariant or a new §7 row cannot leave the audit quietly out of date. Probed three ways
- [x] grep the repository for Cyrillic outside the permitted user-facing strings (`grep -rP '[\x{0400}-\x{04FF}]' --exclude-dir=.git --exclude-dir=.build .`) — zero hits outside `docs/replacements.example.conf`, which is `lint.sh`'s one named exemption because its patterns are Cyrillic by construction. Run with `perl -CSD` rather than `grep -P`: BSD grep, which is what `/usr/bin/grep` is here, has no `-P`
- [x] verify a release build and `bash Scripts/bundle.sh` both succeed from a clean checkout — done in a detached worktree at this commit, with no `.build` and no `Dicta.app` to inherit: `swift build -c release` in 141 s, `bundle.sh` exit 0, and the designated requirement it produced is byte-identical to the one Task 8 measured (`identifier "dev.personal.dicta" and certificate leaf = H"3dcb99…"`), which is the whole property the TCC grant rests on. The gate was run there too and agrees with this checkout: linkage clean, 361 tests, lint clean. The worktree was removed afterwards

### Task 13: Update documentation

- [ ] update `CLAUDE.md`: where the project stands (steps 1–3 done, 4–5 not started), commands, the structure rule, the environment facts as re-measured, and the rules worth not relearning
- [ ] update `SPEC.md`'s status line — it currently reads "specification only, no implementation exists"
- [ ] write `README.md`: install, the keymap snippet, the dictionary file, the `dictactl` verbs, and what the record holds
- [ ] write `docs/manual-checklist.md` with the Post-Completion items below, each with the exact observation that counts as a pass
- [ ] ensure `docs/keymap.snippet.conf` and `docs/replacements.example.conf` match what shipped

## Post-Completion (human verification — §10 and §11, steps 1–3 only)

Not automatable, and the criteria the spec assigns to a person:

- **Step 1** — text appears in the input line of the session the chord was pressed in and the pane focused when the daemon handled the start; the buffer shows it unsubmitted; the hostile canned transcript arrives as a single line with single spaces.
- **Step 2 (a)** — keypress to recording under 150 ms warm, over 10 consecutive attempts (`Scripts/measure.sh`).
- **Step 2 (b)** — a 20-second Russian dictation containing at least two English technical terms produces text whose meaning the user judges correct; the record shows `recognised` non-empty.
- **Step 2 (c)** — stop to injection for a 60-second utterance under 2 s.
- **Step 2 (d)** — the TCC prompt appears once, naming dicta; a rebuild and relaunch do not prompt again.
- **Step 3** — add a deliberately wrong rule, run one dictation, and name the offending rule from `recognised`, `final` and `rules` alone, without re-running anything.
- The chords fire and land in the session they were pressed in, verified against **Claude Code's** input line specifically — not only a fish prompt (F5).
- Sleep/wake and an AirPods route change during an attempt each end it with a visible fault and no injection.
- Daemon lifecycle: survives logout/login; a crash never leaves an indicator claiming a recording that is not happening.

## Not in this plan

- **Step 4** — the external filter command, its configuration and its fallback to **replaced**. The `Filter` seam exists as a `NoFilter` pass-through; nothing invokes a subprocess. D9c defers the choice of engine until real recogniser output has been seen, and that is a decision for the user.
- **Step 5** — the audit giving every row of §7 either an automated test or a checklist entry. Rows belonging to components built here are tested as they are built; the sweep that proves none was missed is step 5's job.
